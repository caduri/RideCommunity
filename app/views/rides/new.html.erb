<% provide(:title, 'Add Ride') %>
<h1>Add Ride</h1>

<div class="row">
  <div class="span6 offset3">
    <%= form_for(@ride) do |f| %>
      <%= render 'fields', f: f %>
      <%= f.submit "Create a Ride", class: "btn btn-large btn-primary" %>
    <% end %>
  </div>
</div>